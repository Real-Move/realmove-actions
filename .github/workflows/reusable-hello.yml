name: Reusable • Hello Workflow

on:
  workflow_call:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from reusable workflow"
      - uses: Real-Move/realmove-actions/hello@main
